/*
    Check that the variable is defined AND is not null
    Check that it is a non-empty string
    Check if is a string
*/
if ((((typeof variableName !== 'undefined' && variableName) && variableName.length > 0)) && typeof variableName.valueOf() === 'string');